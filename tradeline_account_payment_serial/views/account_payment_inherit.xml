<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Payment Form: add voucher serial fields -->
    <record id="view_account_payment_form_serial" model="ir.ui.view">
        <field name="name">account.payment.serial</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="after">
                <field name="is_voucher" invisible="1"/>
                <field name="is_admin_serial" invisible="1"/>
                <field name="lot_id"
                       readonly="state != 'draft' and not is_admin_serial"
                       invisible="not is_voucher"
                       required="is_voucher"
                       options="{'no_create': True, 'no_create_edit': True, 'no_open': True}"/>
            </xpath>
            <xpath expr="//field[@name='journal_id']" position="attributes">
                <attribute name="options">{'no_create': True, 'no_create_edit': True, 'no_open': True}</attribute>
            </xpath>
        </field>
    </record>

    <record id="view_account_register_invoice_form_serial" model="ir.ui.view">
        <field name="name">account.payment.register.serial</field>
        <field name="model">account.payment.register</field>
        <field name="inherit_id" ref="account.view_account_payment_register_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="after">
                <field name="is_voucher" invisible="1"/>
                <field name="is_admin_serial" invisible="1"/>
                <field name="lot_id"
                       invisible="not is_voucher"
                       required="is_voucher"
                       options="{'no_create': True, 'no_create_edit': True, 'no_open': True}"/>
            </xpath>
            <xpath expr="//field[@name='journal_id']" position="attributes">
                <attribute name="options">{'no_create': True, 'no_create_edit': True, 'no_open': True}</attribute>
            </xpath>
        </field>
    </record>



    <!-- Payment Tree: add lot_id column -->
    <record id="account_payment_tree_lot" model="ir.ui.view">
        <field name="name">account.payment.tree.lot</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="after">
                <field name="lot_id" optional="hide"/>
            </xpath>
        </field>
    </record>

    <!-- Payment Search: add lot_id filter -->
    <record id="account_payment_search_lot" model="ir.ui.view">
        <field name="name">account.payment.search.lot</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_search"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="before">
                <field name="lot_id"/>
            </xpath>
        </field>
    </record>

</odoo>
