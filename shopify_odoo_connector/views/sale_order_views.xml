<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--    inherited form view of sale.order-->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.view.form.inherit.shopify.odoo.connector
        </field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="priority">10</field>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button type="object" name="sync_shopify_order"
                        class="oe_highlight" string="Sync"
                        invisible="shopify_instance_id == False"/>
                <field name="shopify_order_ref" invisible="1"/>
            </xpath>
            <xpath expr="//notebook/page[@name='other_information']"
                   position="after">
                <page string="Shopify Info" name="shopify_details">
                    <group>
                        <field name="shopify_instance_id"/>
                    </group>
                        <field name="shopify_sync_ids" nolabel="1" readonly="1"
                               invisible="shopify_instance_id == False">
                            <list>
                                <field name="instance_id"/>
                                <field name="shopify_order_ref"/>
                                <field name="synced_order" optional="hide"/>
                                <field name="shopify_order_name"/>
                                <field name="shopify_order_number"/>
                                <field name="order_status" optional="hide"/>
                                <field name="last_order_id" optional="hide"/>
                                <field name="payment_status" optional="hide"/>
                                <field name="fulfillment_status"
                                       optional="hide"/>
                            </list>
                        </field>
                </page>
            </xpath>
        </field>
    </record>
    <!--    List view for the Shopify order menus: the standard sale order
            list plus the Shopify order number. mode="primary" makes this a
            standalone view used only by the two actions below, so the
            ordinary Sales list is left exactly as it is. -->
    <record id="shopify_sale_order_view_tree" model="ir.ui.view">
        <field name="name">sale.order.list.shopify</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="mode">primary</field>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="reference_number" string="Shopify Number"
                       optional="show"/>
            </xpath>
        </field>
    </record>
    <!--    Matching search view, so the Shopify number can be typed into the
            search box on those same two menus. -->
    <record id="shopify_sale_order_view_search" model="ir.ui.view">
        <field name="name">sale.order.search.shopify</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter"/>
        <field name="mode">primary</field>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="reference_number" string="Shopify Number"
                       filter_domain="[('reference_number', 'ilike', self)]"/>
            </xpath>
        </field>
    </record>
    <!--    action for sale.order-->
    <record id="shopify_sale_order_action" model="ir.actions.act_window">
        <field name="name">Shopify Orders</field>
        <field name="res_model">sale.order</field>
        <field name="domain">[('shopify_sync_ids', '!=', False), ('state', '!=',
            'draft')]
        </field>
        <field name="view_mode">list,form</field>
        <field name="target">current</field>
        <field name="view_id" ref="shopify_sale_order_view_tree"/>
        <field name="search_view_id" ref="shopify_sale_order_view_search"/>
        <field name="context">{'create': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No orders found
            </p>
        </field>
    </record>
    <!--    action for sale.order in draft-->
    <record id="shopify_draft_sale_order_action" model="ir.actions.act_window">
        <field name="name">Shopify Draft Orders</field>
        <field name="res_model">sale.order</field>
        <field name="domain">[('shopify_sync_ids', '!=', False), ('state', '=',
            'draft')]
        </field>
        <field name="view_mode">list,form</field>
        <field name="target">current</field>
        <field name="view_id" ref="shopify_sale_order_view_tree"/>
        <field name="search_view_id" ref="shopify_sale_order_view_search"/>
        <field name="context">{'create': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No orders found
            </p>
        </field>
    </record>
    <!--    menu for sale.order
            These three are the only Shopify menus a Shopify / User sees.
            group_shopify_manager implies group_shopify_user, so a manager
            gets them as well. -->
    <menuitem id="sale_order_menu_root" name="Orders"
              parent="shopify_odoo_connector.shopify_odoo_connector_menu_root"
              sequence="3"
              groups="shopify_odoo_connector.group_shopify_user"/>
    <!--Draft orders menu-->
    <menuitem id="shopify_draft_sale_order_menu_root" name="Draft Orders"
              parent="sale_order_menu_root"
              sequence="1"
              groups="shopify_odoo_connector.group_shopify_user"
              action="shopify_draft_sale_order_action"/>
    <!--Orders menu-->
    <menuitem id="sale_order_menu" name="Orders"
              parent="shopify_odoo_connector.sale_order_menu_root"
              sequence="2"
              groups="shopify_odoo_connector.group_shopify_user"
              action="shopify_sale_order_action"/>
</odoo>
